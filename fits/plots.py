import matplotlib.pyplot as plt


def plotter(x, y, out.params, title)
    fit = Decay_formula(out.params, x)    
    plt.figure()
    plt.plot(x,y,'o')
    plt.plot(x,fit,linewidth=5, alpha=0.5)
    plt.title(title)
    plt.show()
    
    
    
    

